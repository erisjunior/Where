import { styled } from '@mui/material'

export const Root = styled('div')(() => ({
  width: '100vw',
  height: '100vh',
  display: 'flex',
  overflow: 'hidden',
  position: 'relative',
  flexDirection: 'column',
  boxSizing: 'border-box'
}))
